# Production Environment Configuration Template
# Copy this file to .env.production and fill in actual values

# ==================== DATABASE ====================
DATABASE_URL=postgresql+asyncpg://botuser:CHANGE_ME_STRONG_PASSWORD@postgres:5432/training_bot
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ==================== REDIS ====================
# Sentinel mode (recommended for production)
REDIS_SENTINEL_NODES=redis-sentinel1:26379,redis-sentinel2:26379,redis-sentinel3:26379
REDIS_MASTER_NAME=mymaster
REDIS_FSM_DB=0
REDIS_THROTTLE_DB=1

# Alternative: Simple mode (development only)
# REDIS_URL=redis://localhost:6379

# ==================== TELEGRAM BOT ====================
BOT_TOKEN=YOUR_BOT_TOKEN_FROM_@BOTFATHER
ADMIN_PASSWORD_HASH=CHANGE_ME_BCRYPT_HASH

# Generate admin password hash:
# python -c "import bcrypt; print(bcrypt.hashpw(b'your_password', bcrypt.gensalt()).decode())"

# ==================== MONITORING ====================
SENTRY_DSN=YOUR_SENTRY_DSN_URL
GRAFANA_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ==================== ENVIRONMENT ====================
ENVIRONMENT=production
LOG_LEVEL=INFO

# ==================== PERFORMANCE ====================
REQUEST_TIMEOUT=30
MAX_CONCURRENT_TASKS=1000
CLEANUP_INTERVAL=60

# ==================== SECURITY ====================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_RANDOM_32_CHARS

# ==================== NOTES ====================
# 1. Never commit .env.production to git!
# 2. Use strong passwords (min 16 characters, mixed case, numbers, symbols)
# 3. Rotate credentials regularly (every 90 days)
# 4. Store backups of credentials securely
# 5. Use environment-specific Sentry projects
